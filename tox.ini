[tox]
project = photoshell
envlist = py34,pypy

[testenv]
deps =
    -rrequirements.txt
    coverage
    pytest
    mock
commands =
    coverage erase
    coverage run -m pytest tests
    coverage report --show-missing
